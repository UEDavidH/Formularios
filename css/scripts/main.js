'use strict';
const btn = document.getElementById('boton2');

btn.addEventListener('click',(e)=>{
    e.preventDefault();
    const form = document.getElementById('form');
    console.log(form);
});//2do parámetro función tipo flecha anónima